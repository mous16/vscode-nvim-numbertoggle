name: panvimdoc

on:
  push:
    branches:
      - main
    paths:
      - README.md
      - doc/vscode-numbertoggle.txt

jobs:
  docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          token: ${{ secrets.PAT }}

      - name: Generate docs
        uses: kdheepak/panvimdoc@main
        with:
          vimdoc: vscode-numbertoggle
          description: automatic toggle between hybrid and absolute line numbers in VSCode

      - uses: stefanzweifel/git-auto-commit-action@v7
        with:
          commit_message: "docs(vimdoc): auto generate from readme"
          branch: ${{ github.head_ref }}
